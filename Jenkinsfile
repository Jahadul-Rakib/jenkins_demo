pipeline{
    agent any
    stages {
            stage ('Build') {
                steps {
                        echo 'Running build phase...'
                        sh 'pwd'
                        sh 'ls'
                }
            }
    }
}
